import React from 'react';
import ReactDOM from 'react-dom';
import UserProfilePage from './user-profile-page';  // Import your user profile page

const root = ReactDOM.createRoot(document.getElementById('root'));

root.render(
  <React.StrictMode>
    <UserProfilePage />
  </React.StrictMode>
);